{
  "name": "adaptive-learning-platform",
  "version": "1.0.0",
  "description": "Full-Stack Adaptive Learning Platform with MAPS Diagnostic Engine",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "node server/src/index.js",
    "server": "npm --prefix server start",
    "server:dev": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "test:e2e": "node server/test-e2e.js",
    "reset:demo": "node server/reset-demo.js"
  }
}
